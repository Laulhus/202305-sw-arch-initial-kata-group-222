const MISSING_PASSWORD_ERROR_MSG = 'Must provide a password';
const PASSWORD_SHORT_ERROR_MSG = 'Password must be at least 8 characters';
const PASSWORD_MUST_CONTAIN_NUMBERS = 'Password must contain at least 2 numbers';
const PASSWORD_MUST_CONTAIN_CAPITAL = 'Password must contain at least one capital letter';
const PASSWORD_MUST_CONTAIN_SPECIAL = 'Password must contain at least one special character';

export {
  MISSING_PASSWORD_ERROR_MSG,
  PASSWORD_SHORT_ERROR_MSG,
  PASSWORD_MUST_CONTAIN_NUMBERS,
  PASSWORD_MUST_CONTAIN_CAPITAL,
  PASSWORD_MUST_CONTAIN_SPECIAL,
};
